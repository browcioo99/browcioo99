Hello! Below you will find my board game python code for charts analyzing data 😄
https://www.kaggle.com/datasets/mrpantherson/board-game-data?datasetId=1085&sortBy=dateRun&tab=profile - dataset 

import pandas as pd
import io
import plotly.express as px
import plotly.graph_objects as go

#importing dataset
bg = pd.read_excel(r'/Users/wiktoriabrowarska/Desktop/PythonScripts/data analysis/bgg/bgg_db.xlsx')

#chart1

#sorting values to show top 20 by geek rating
top_20 = bg.sort_values('avg_rating', ascending=False).head(20).round(1)

# Convert the 'year' column to string to treat it as a category
top_20['year'] = top_20['year'].astype(str)

# Creating the bar chart
rating = px.bar(
    top_20,
    x = 'avg_rating',
    y = 'names',
    title = 'Top 20 best-ranked games by Board Game Geek',
    height = 600,
    color = 'year',
    category_orders = {'year': ['2010', '2011', '2012', '2013', '2014', '2015', '2016', '2017', '2018', '2019', '2020', '2021']},
    color_discrete_map = {'year': 'blue'}  # Set a fixed color for the 'year' values
)

# Customizing the chart layout
rating.update_layout(
    font_family = "Rockwell",
    xaxis_range = [8.5, 9.5],
    xaxis = dict(dtick = 0.1),
    yaxis = dict(dtick = 1),
    title = dict(font = dict(size = 20)),
    legend = dict(
        title = None,
        orientation = "h",
        y = 1,
        yanchor = "bottom",
        x = 0.5,
        xanchor = "center"
    )
)
rating.show()

#chart2

#grouping data to show how much games were made by year
rank_count_bg = bg.groupby('year').size().reset_index(name = 'number of board games')

#creating chart
amount_by_year = px.line(rank_count_bg, x = 'year', y = 'number of board games',title = 'Number of board games made each year')
amount_by_year.update_layout(
    xaxis_range = [2000,2017],
    xaxis = dict(dtick = 1),
    font_family = "Courier New",
    font_color = "seagreen",
    title_font_family = "Courier New",
    title_font_color = "green",
    title = dict(font = dict(size = 20))
)
amount_by_year.update_traces(line_color = 'purple',line = {'width': 5})
amount_by_year.show()

#chart3

# Combine all columns containing 'category'
combined_categories = pd.concat([bg[col] for col in bg.columns if 'category' in col])

# Count the occurrences of each category
category_counts = combined_categories.value_counts().reset_index()
category_counts.columns = ['category', 'count']

# Filter categories with counts greater than 200
filtered_categories_count = category_counts[category_counts['count'] > 200]

# Create the bar chart using the filtered data
category = go.Figure(go.Bar(
    x=filtered_categories_count['category'],
    y=filtered_categories_count['count']
))

category.update_layout(
    title='How many games of each category were made (Categories with more than 200 counts)',
    xaxis=dict(
        title='Category',
        tickangle=-45,
        tickfont=dict(size=14),
        tickmode='array',  # Use custom tick values
        tickvals=list(filtered_categories_count['category']),  # Specify the tick values
    ),
    yaxis=dict(title='Count'),
    font_family="Helvetica",
    font_color="blue",
    width=1000,  # Increase width for better visibility
    height=500,
    bargap=0.1,
    bargroupgap=0.1
)

category.show()
